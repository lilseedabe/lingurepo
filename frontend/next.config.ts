import type { NextConfig } from "next";

// Next.js configuration options
const nextConfig: NextConfig = {
  // フロントエンドを `frontend` 配下に移動したため、特別なパス設定は不要です。
  // ここに必要に応じて設定オプションを追加します。
};

export default nextConfig;

